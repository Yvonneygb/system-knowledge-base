# 修复：【其他常见问题】出现在每个 TAB 下方

## 现象
修复【关联依赖】空白后，【其他常见问题】模块（含 Q1-Q6 卡片）出现在每一个 tab 内容的下方，而非仅「常见问题 FAQ」tab。

## 根因
上一轮修 div 平衡时，把 detail-logic / troubleshoot 漏关的 `<div>` 在正确位置补上了，导致 `<div id="faq">` 的嵌套关系暴露：
- `id="faq"` 在 index.md 第 938 行打开，但 detail-logic 的容器此时仍未闭合 → faq 被嵌套在 detail-logic 内部。
- faq 内容分成两部分：报错对照表（在 `id="faq"` 内，正常随 FAQ tab 切换）+ 其他常见问题 / Q 卡片（`faq-qa-section`，在 `id="faq"` 关闭之后打开，是顶层兄弟节点，**没有 display 切换**）→ 永远可见，所以每个 tab 下都显示。

## 修复（安全方案，不做 div 手术）
1. `index.md` 第 1197 行：`<div class="faq-qa-section">` → `<div id="faq-qa" class="faq-qa-section">`，给该模块一个 id 以便切换。
2. `BreadcrumbTabs.vue` 的 `applyTabVisibility()`：在原有按 id 切换逻辑之后，额外把 `faq-qa` 的 display 与 `faq` tab 绑定（faq 激活时显示，否则隐藏）。

未改动任何 `<div>` 开闭，仅增加 id 属性与一段 JS，div 全局/各 section 平衡保持不变，构建零风险。

## 验证
- `vitepress build docs` 成功：build complete in 10.15s
- dist `家装真实性核销/index.html` 含「其他常见问题」= True（内容仍在，仅通过 display 控制显隐）

## 文件
- `docs/家装管理/项目往来/家装真实性核销/index.md`（faq-qa-section 加 id）
- `docs/.vitepress/theme/BreadcrumbTabs.vue`（applyTabVisibility 增加 faq-qa 切换）
